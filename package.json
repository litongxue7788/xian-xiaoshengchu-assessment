{
  "name": "xian-xiaoshengchu-assessment",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/ || echo 'Build complete'",
    "vercel-build": "mkdir -p public && cp index.html public/ || echo 'Vercel build complete'"
  }
}
